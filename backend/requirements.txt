# Core
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
sqlmodel>=0.0.16

# Verification engine
sympy>=1.12
pint>=0.23
numpy>=1.26

# Utilities
httpx>=0.27
python-dotenv>=1.0

# Optional: Postgres driver (only needed if DATABASE_URL points at Postgres)
# psycopg[binary]>=3.1

# Optional LLM providers (only needed if LLM_PROVIDER != mock)
# openai>=1.30
# anthropic>=0.34

# Dev
pytest>=8.0
pytest-timeout>=2.3
